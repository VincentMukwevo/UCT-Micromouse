# Dedicated Python Development Folder

Place all of your library files and your `main.py` in this folder. 
When you run `python tools/deploy.py -e micropython`, this entire folder will be mirrored natively to the mouse's flash storage.
